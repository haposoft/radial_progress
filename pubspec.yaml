name: radial_progress
description: "A Flutter library for displaying progress as a circle chart"
version: 0.1.0
homepage: https://github.com/shervin-h/radial_progress
repository: https://github.com/shervin-h/radial_progress
issue_tracker: https://github.com/shervin-h/radial_progress/issues

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
